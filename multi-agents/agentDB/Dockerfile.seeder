FROM python:3.11-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /app

RUN pip install --no-cache-dir redis==5.0.1

COPY seed_agent_data.py seed_agent_data.py
COPY seed_data.txt seed_data.txt

CMD ["python", "seed_agent_data.py"]
